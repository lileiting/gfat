package Gfat::Config;
use warnings;
use strict;
use vars qw(@EXPORT @EXPORT_OK);
use base qw(Exporter);
@EXPORT = qw();
@EXPORT_OK = qw($max_textwidth $VERSION);

our $VERSION = '0.1.226';
our $max_textwidth = 70;

